{
  "devDependencies": {
    "@types/node": "^26.2.0",
    "cypress": "^15.21.0",
    "dotenv": "^17.4.2",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox",
    "cy:run:edge": "cypress run --browser edge",
    "cy:run:all": "npm run cy:run:chrome && npm run cy:run:firefox && npm run cy:run:edge"
  }
}
